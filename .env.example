# React App Environment Variables
# Copy this file to .env and update the values

# Mapbox API Token - Get from https://account.mapbox.com/access-tokens/
# This is injected at runtime, not build time
REACT_APP_MAPBOX_TOKEN=YOUR_MAPBOX_ACCESS_TOKEN

# API Base URL for the frontend (should match Apache proxy configuration)
REACT_APP_API_BASE_URL=/heatmap/api

# Public URL path for the React app
PUBLIC_URL=/heatmap

# Admin API Key for accessing download logs export and the pageview log (/api/pageviews)
ADMIN_API_KEY=secure_random_key

PYTHONPATH=.
